import React from 'react';
import PortfolioList from './PortfolioList';

const PortfolioCards = (props) =>{
    console.log(props)
    return(
      <PortfolioList className= 'portfolioBody'/>
    );
};

export default PortfolioCards;